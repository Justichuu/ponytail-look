# Rung 4 looks

Ponytail's ladder, rung 4:

> Native platform feature covers it? `<input type="date">` over a picker lib.

That rung is how the date picker goes from 404 lines to 23. It is also a
guess. "Look before you write" in the same file means grep. The oval
glasses never see the running page.

This paragraph lives under the ladder in `skills/ponytail/SKILL.md`.
One skill. Not a second ladder.

On a world surface: look with the host's eyes. BLIND or UNSEEN — do not
edit. Fail closed. If the witness already shows the native control,
delete.

Test it: `npm run try`.
